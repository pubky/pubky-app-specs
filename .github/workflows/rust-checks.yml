name: Rust Checks

on:
  workflow_dispatch:
  push:
    branches: ["main", "v1"]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Check formatting
        run: cargo fmt -- --check

  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
          targets: wasm32-unknown-unknown

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: "lint"
          cache-on-failure: true

      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

  package:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: "package"
          cache-on-failure: true

      - name: Verify crates.io package
        run: cargo publish --dry-run

  old-names:
    name: no pre-rename identifiers
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: '! git grep -n -E "PubkyApp[A-Za-z]|pubky_app_specs" -- src tests examples README.md docs/UNICODE_NOTES.md'
